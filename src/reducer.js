import { useReducer } from "react";
import {
  CLEAR_CART,
  REMOVE,
  INCREASE,
  DECREASE,
  DISPLAY_ITEMS,
  LOADING,
} from "./actions";

const reducer = (state, action) => {
  return state;
};

export default reducer;
